name = "solr-arc-pog-agent"
main = "index.ts"
compatibility_date = "2024-01-01"

# Environment variables (secrets should be set via: wrangler secret put VARIABLE_NAME)
[vars]
ARC_RPC_URL = "https://rpc.testnet.arc.network"
MINTING_CONTROLLER_ADDRESS = "" # Fill after deployment
REGISTRY_ADDRESS = "" # Fill after deployment

# Set these secrets via CLI:
# wrangler secret put AI_AGENT_PRIVATE_KEY
# wrangler secret put PINATA_JWT
# wrangler secret put NREL_API_KEY
